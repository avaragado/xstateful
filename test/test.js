// @flow

const howLongTillLunch = require('..');

describe('wibble', () => {
    test('a', () => {
        expect(howLongTillLunch).toBeDefined();
    });

    test('b', () => {
        expect(2).toBe(2);
    });
});
